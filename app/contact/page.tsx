'use client'
import { FormEvent, useState } from 'react'
import { PageShell } from '@/components/page-shell'
export default function ContactPage() { const [sent, setSent] = useState(false); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); const form = new FormData(event.currentTarget); await fetch('/api/leads', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(form)) }); setBusy(false); setSent(true) } return <PageShell><main><section className="page-hero"><p className="eyebrow">CONTACT / PROJECT INTAKE</p><h1>Bring us the<br /><span>interesting problem.</span></h1><p>Tell us what you are building, what is in the way, and where a sharper technical partner would create leverage.</p></section>{sent ? <section className="content-section"><div className="page-card wide"><p className="eyebrow">MESSAGE RECEIVED</p><h2>We&apos;ll be in touch shortly.</h2><p>Your project context is safely in our queue. Prefer a calendar? <a href="https://calendly.com" target="_blank" rel="noreferrer">Open Calendly ↗</a></p></div></section> : <form className="contact-form" onSubmit={submit}><div className="form-row"><label>Name<input name="name" required placeholder="Your name" /></label><label>Email<input name="email" type="email" required placeholder="you@company.com" /></label></div><div className="form-row"><label>Company<input name="company" placeholder="Company or team" /></label><label>Budget<select name="budget" defaultValue=""><option value="" disabled>Select a range</option><option>Under $25k</option><option>$25k–$75k</option><option>$75k+</option></select></label></div><label>Project type<select name="projectType" defaultValue="Full-stack web"><option>Full-stack web</option><option>Generative AI</option><option>Cloud & DevOps</option><option>Something else</option></select></label><label>Tell us more<textarea name="message" required placeholder="What are you trying to make possible?" /></label><button className="button button-primary" disabled={busy}>{busy ? 'Sending…' : 'Send project context ↗'}</button></form>}</main></PageShell> }
