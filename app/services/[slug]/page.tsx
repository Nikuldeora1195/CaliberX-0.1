import Link from 'next/link'
import { notFound } from 'next/navigation'
import { PageShell } from '@/components/page-shell'

const data: Record<string, { title: string; kicker: string; body: string; bullets: string[] }> = { 'full-stack-web-development': { title: 'Full-stack products with staying power.', kicker: '01 / FULL-STACK WEB DEVELOPMENT', body: 'We build the product layer and the system beneath it together, so the interface is fast, the architecture is legible, and the team can keep shipping after handoff.', bullets: ['Product discovery and technical architecture', 'Next.js, TypeScript, APIs, and Postgres', 'Performance, accessibility, and deployment'] }, 'generative-ai-and-automation': { title: 'AI that earns its place in the workflow.', kicker: '02 / GENERATIVE AI & AUTOMATION', body: 'We turn language models into dependable product capabilities with retrieval boundaries, evaluations, tool use, and human-centered interfaces.', bullets: ['RAG and knowledge interfaces', 'Agentic workflows and tool calling', 'Evaluation, observability, and safety'] }, 'cloud-infrastructure-devops': { title: 'Infrastructure that makes ambition feel calm.', kicker: '03 / CLOUD INFRASTRUCTURE & DEVOPS', body: 'We design the operational foundation around your actual constraints: secure environments, resilient data, and deployments your team can understand.', bullets: ['Cloud architecture and containerization', 'CI/CD, monitoring, and incident readiness', 'Security reviews and scaling plans'] } }
export default async function ServiceDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const service = data[slug]; if (!service) notFound(); return <PageShell><main><section className="page-hero"><p className="eyebrow">{service.kicker}</p><h1>{service.title}</h1><p>{service.body}</p><Link className="button button-primary" href="/contact">Talk through your build <span>↗</span></Link></section><section className="page-grid">{service.bullets.map((bullet) => <article className="page-card" key={bullet}><p className="card-label">CAPABILITY</p><h3>{bullet}</h3><p>Designed with clear ownership, useful documentation, and a path to measurable outcomes.</p></article>)}</section></main></PageShell> }
