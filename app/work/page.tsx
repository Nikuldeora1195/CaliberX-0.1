import Link from 'next/link'
import { PageShell } from '@/components/page-shell'
const projects = [{ slug: 'northstar-ai', title: 'Northstar AI', category: 'GEN AI / PLATFORM', summary: 'A decision workspace that turns fragmented operational knowledge into clear next actions.', year: '2025' }, { slug: 'atlas-cloud', title: 'Atlas Cloud', category: 'CLOUD / INFRASTRUCTURE', summary: 'A resilient data platform for teams operating across complex environments.', year: '2024' }, { slug: 'signal-commerce', title: 'Signal Commerce', category: 'WEB / GROWTH', summary: 'A high-conversion commerce system built around speed, clarity, and intelligent automation.', year: '2024' }]
export default function WorkPage() { return <PageShell><main><section className="page-hero"><p className="eyebrow">WORK / SELECTED SYSTEMS</p><h1>Built for the<br /><span>hard parts.</span></h1><p>Case studies in thoughtful architecture, practical AI, and products that create momentum after launch.</p></section><section className="work-list">{projects.map((project) => <Link href={`/work/${project.slug}`} className="work-card" key={project.slug}><div><p className="card-label">{project.category} / {project.year}</p><h2>{project.title}</h2><p>{project.summary}</p></div><span className="card-arrow">↗</span></Link>)}</section></main></PageShell> }
