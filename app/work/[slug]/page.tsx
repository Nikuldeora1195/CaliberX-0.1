import { notFound } from 'next/navigation'
import { PageShell } from '@/components/page-shell'
const projects: Record<string, { title: string; category: string; summary: string; result: string }> = { 'northstar-ai': { title: 'Northstar AI', category: 'GEN AI / PLATFORM', summary: 'A decision workspace that turns fragmented operational knowledge into clear next actions.', result: 'Reduced research cycles from days to minutes with a grounded retrieval layer and carefully designed review states.' }, 'atlas-cloud': { title: 'Atlas Cloud', category: 'CLOUD / INFRASTRUCTURE', summary: 'A resilient data platform for teams operating across complex environments.', result: 'Created a clearer deployment path with observable services, secure data boundaries, and a calm incident surface.' }, 'signal-commerce': { title: 'Signal Commerce', category: 'WEB / GROWTH', summary: 'A high-conversion commerce system built around speed, clarity, and intelligent automation.', result: 'Connected a faster storefront to an automated growth engine without adding operational drag.' } }
export default async function CaseStudy({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const project = projects[slug]; if (!project) notFound(); return <PageShell><main><section className="page-hero"><p className="eyebrow">{project.category}</p><h1>{project.title}<br /><span>under pressure.</span></h1><p>{project.summary}</p></section><section className="page-grid"><article className="page-card wide"><p className="card-label">THE OUTCOME</p><h2>{project.result}</h2></article><article className="page-card"><p className="card-label">APPROACH</p><p>We mapped the constraint, established a small set of durable primitives, and delivered in visible increments with the client team close to every decision.</p></article></section></main></PageShell> }
